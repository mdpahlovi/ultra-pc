"use client";

import { Layout, Badge, Button, Card } from "antd";
const { Header, Footer, Content } = Layout;
const { Ribbon } = Badge;

export { Layout, Header, Footer, Content, Badge, Ribbon as BadgeRibbon, Button, Card };
